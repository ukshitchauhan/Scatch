<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch | Admin Panel</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'scatch-purple': '#a855f7', // Ensuring your custom color works
                    }
                }
            }
        }
    </script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
    <style>
        /* Custom scrollbar for admin panel */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #18181b; } 
        ::-webkit-scrollbar-thumb { background: #3f3f46; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #52525b; }
    </style>
</head>
<body class="bg-zinc-900 text-white font-sans antialiased selection:bg-purple-500 selection:text-white">

    <nav class="w-full h-16 bg-zinc-900 border-b border-zinc-800 flex items-center justify-between px-6 md:px-10 sticky top-0 z-50">
        
        <div class="flex items-center gap-4">
            <a href="/admin/dashboard" class="text-2xl font-bold tracking-tighter flex items-center gap-3">
                
                <div>
                    <span class="text-white">SCA</span><span class="text-scatch-purple drop-shadow-[0_0_8px_rgba(168,85,247,0.8)]">TCH</span>
                </div>

                <span class="text-xs px-2 py-1 rounded bg-purple-600/20 text-purple-400 border border-purple-500/20 font-medium tracking-normal">
                    ADMIN
                </span>
            </a>
        </div>

        <div class="flex items-center gap-6">
            
            <a href="/" target="_blank" class="hidden md:flex items-center gap-2 text-xs font-medium text-zinc-400 hover:text-white transition-colors">
                <i class="ri-external-link-line"></i> View Live Shop
            </a>

            <div class="h-6 w-[1px] bg-zinc-800 hidden md:block"></div>

            <button class="relative text-zinc-400 hover:text-white transition-colors">
                <i class="ri-notification-3-line text-lg"></i>
                <span class="absolute top-0 right-0 w-2 h-2 bg-purple-500 rounded-full border border-zinc-900"></span>
            </button>

            <div class="flex items-center gap-3">
                <div class="text-right hidden md:block">
                    <p class="text-sm font-medium text-white">Vyom Chauhan</p>
                    <p class="text-xs text-zinc-500">Super Admin</p>
                </div>
                
                <div class="w-9 h-9 rounded-full bg-zinc-800 border border-zinc-700 flex items-center justify-center overflow-hidden">
                    <span class="font-bold text-sm text-purple-400">VC</span>
                </div>
                
                <a href="/admin/logout" class="ml-2 text-zinc-500 hover:text-red-400 transition-colors" title="Logout">
                    <i class="ri-logout-box-r-line text-lg"></i>
                </a>
            </div>
        </div>
    </nav>